#include "Monster.h"

void Monster::TurnStart()
{
	/*
	int selectSkill;
	vector<int> skills = GetSkills();
	do {
		selectSkill = rand() % skills.size();
		auto it = 
	}while ([selectSkill]);
	dataTable.GetSkill(skills[selectSkill]);
	*/
}
